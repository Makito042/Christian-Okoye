import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { CartProvider } from "@/context/CartContext";
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";
import { CartDrawer } from "@/components/store/CartDrawer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Christian Okoye | The Nigerian Nightmare | Official Site & Store",
  description: "Official website of Kansas City Chiefs legend, 1989 NFL Rushing Champion, and Chiefs Hall of Famer Christian Okoye. Shop authentic autographed memorabilia and book appearances.",
  keywords: ["Christian Okoye", "The Nigerian Nightmare", "Kansas City Chiefs", "Chiefs Hall of Fame", "Signed Memorabilia", "Autographed Jersey"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#FAF8F5] text-[#161413] selection:bg-[#C8102E] selection:text-white">
        <CartProvider>
          <Navbar />
          <main className="flex-1">{children}</main>
          <Footer />
          <CartDrawer />
        </CartProvider>
      </body>
    </html>
  );
}
